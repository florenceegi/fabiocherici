---
import Layout from '../../../layouts/Layout.astro';
import OracodeContent from '../../../components/pages/OracodeContent.astro';
import { getLangFromUrl, t } from '../../../i18n/translations';

const lang = getLangFromUrl(Astro.url);
---

<Layout title="Oracode — Fabio Cherici" description={t(lang, 'meta.oracodeDescription')} slug="oracode">
  <OracodeContent lang={lang} />
</Layout>
